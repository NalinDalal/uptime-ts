import type { NextConfig } from "next";

/**
 * Next.js application configuration for the web frontend.
 *
 * Currently uses the default configuration. Extend this object with custom
 * settings (e.g. `reactStrictMode`, `images`, `experimental` flags) as needed.
 *
 * @type {NextConfig}
 */
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
